import React from "react";

const NotFound = () => {
  return (
    <div
      className="notfound"
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "40",
        fontWeight: "600",
      }}
    >
      PAGE NOT FOUND
    </div>
  );
};

export default NotFound;
